import React from 'react';
import './Details.css';

const Details = (props: any) => {
    debugger;
    return (
        <div>
            {
                `DETAILS for RECORD #${props.id}`
            }
        </div>
    );
};

export default Details;